import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mimo.medstracker',
  appName: 'meds-tracker-mimo',
  webDir: '.output/public'
};

export default config;
